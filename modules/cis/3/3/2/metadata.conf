#!/usr/bin/env bash
# CIS 3.3.2 - Ensure packet redirect sending is disabled.
# SPDX-License-Identifier: MIT

# shellcheck disable=SC2034 # Metadata is consumed by lib/module_api.sh.
RLCH_MODULE_ID="3.3.2"
RLCH_MODULE_TITLE="Ensure packet redirect sending is disabled"
RLCH_MODULE_DESCRIPTION="Disables IPv4 packet redirect sending for all and default interfaces in the running kernel and an isolated persistent sysctl configuration."
RLCH_MODULE_RATIONALE="ICMP redirects are unnecessary on hosts that do not act as routers and can expose information about network topology."
RLCH_MODULE_LEVEL="1"
RLCH_MODULE_ENABLED="true"
RLCH_MODULE_REQUIRES_REBOOT="false"
RLCH_MODULE_OPENSCAP_RULE="manual"
