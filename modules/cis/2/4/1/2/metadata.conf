#!/usr/bin/env bash
#
# Rocky Linux CIS Hardening Framework
# CIS 2.4.1.2 - Ensure permissions on /etc/crontab are configured.
# SPDX-License-Identifier: MIT
#

# shellcheck disable=SC2034 # Metadata is consumed by lib/module_api.sh.

RLCH_MODULE_ID="2.4.1.2"
RLCH_MODULE_TITLE="Ensure permissions on /etc/crontab are configured"
RLCH_MODULE_DESCRIPTION="Ensures /etc/crontab is owned by root:root and has mode 0600 or more restrictive."
RLCH_MODULE_RATIONALE="Restricting ownership and permissions on /etc/crontab prevents unauthorized users from modifying scheduled system jobs."
RLCH_MODULE_LEVEL="1"
RLCH_MODULE_ENABLED="true"
RLCH_MODULE_REQUIRES_REBOOT="false"
RLCH_MODULE_OPENSCAP_RULE="manual"
