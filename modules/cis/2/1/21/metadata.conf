#!/usr/bin/env bash
#
# Rocky Linux CIS Hardening Framework
# CIS 2.1.21 - Ensure mail transfer agents are configured for local-only mode.
# SPDX-License-Identifier: MIT
#

# shellcheck disable=SC2034 # Metadata is consumed by lib/module_api.sh.

RLCH_MODULE_ID="2.1.21"
RLCH_MODULE_TITLE="Ensure mail transfer agents are configured for local-only mode"
RLCH_MODULE_DESCRIPTION="Ensures Postfix is configured for loopback-only operation and no MTA listens on a non-loopback address on TCP ports 25, 465, or 587."
RLCH_MODULE_RATIONALE="Restricting mail transfer agents to loopback interfaces reduces exposure to remote attacks while preserving local mail delivery for system services."
RLCH_MODULE_LEVEL="1"
RLCH_MODULE_ENABLED="true"
RLCH_MODULE_REQUIRES_REBOOT="false"
RLCH_MODULE_OPENSCAP_RULE="manual"
