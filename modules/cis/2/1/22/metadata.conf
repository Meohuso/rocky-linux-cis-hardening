#!/usr/bin/env bash
#
# Rocky Linux CIS Hardening Framework
# CIS 2.1.22 - Ensure only approved services are listening on a network interface.
# SPDX-License-Identifier: MIT
#

# shellcheck disable=SC2034 # Metadata is consumed by lib/module_api.sh.

RLCH_MODULE_ID="2.1.22"
RLCH_MODULE_TITLE="Ensure only approved services are listening on a network interface"
RLCH_MODULE_DESCRIPTION="Inventories listening TCP and UDP services for manual comparison with the organization's approved service baseline. The framework does not infer which services are authorized."
RLCH_MODULE_RATIONALE="Unapproved network listeners increase the attack surface and may expose unnecessary or unauthorized services to remote systems."
RLCH_MODULE_LEVEL="1"
RLCH_MODULE_ENABLED="true"
RLCH_MODULE_REQUIRES_REBOOT="false"
RLCH_MODULE_OPENSCAP_RULE="manual"
