#!/usr/bin/env bash
# CIS 5.1.1 - Ensure permissions on /etc/ssh/sshd_config are configured.
# SPDX-License-Identifier: MIT

# shellcheck disable=SC2034 # Metadata is consumed by lib/module_api.sh.
RLCH_MODULE_ID="5.1.1"
RLCH_MODULE_TITLE="Ensure permissions on /etc/ssh/sshd_config are configured"
RLCH_MODULE_DESCRIPTION="Ensures /etc/ssh/sshd_config is owned by root:root and has mode 0600 or more restrictive."
RLCH_MODULE_RATIONALE="Restricting access to the SSH daemon configuration prevents unauthorized changes to remote-access security settings."
RLCH_MODULE_LEVEL="1"
RLCH_MODULE_ENABLED="true"
RLCH_MODULE_REQUIRES_REBOOT="false"
RLCH_MODULE_OPENSCAP_RULE="manual"
