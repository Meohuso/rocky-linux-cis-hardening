#!/usr/bin/env bash
# CIS 4.2.2 - Ensure firewalld loopback traffic is configured.
# SPDX-License-Identifier: MIT

# shellcheck disable=SC2034 # Metadata is consumed by lib/module_api.sh.
RLCH_MODULE_ID="4.2.2"
RLCH_MODULE_TITLE="Ensure firewalld loopback traffic is configured"
RLCH_MODULE_DESCRIPTION="Trusts the loopback interface and drops spoofed IPv4 and IPv6 loopback-source traffic through firewalld."
RLCH_MODULE_RATIONALE="Legitimate local loopback traffic must remain available while packets spoofing loopback source addresses outside loopback are rejected."
RLCH_MODULE_LEVEL="1"
RLCH_MODULE_ENABLED="true"
RLCH_MODULE_REQUIRES_REBOOT="false"
RLCH_MODULE_OPENSCAP_RULE="manual"
