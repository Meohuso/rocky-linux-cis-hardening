#!/usr/bin/env bash
# CIS 4.2.1 - Ensure firewalld drops unnecessary services and ports.
# SPDX-License-Identifier: MIT

# shellcheck disable=SC2034 # Metadata is consumed by lib/module_api.sh.
RLCH_MODULE_ID="4.2.1"
RLCH_MODULE_TITLE="Ensure firewalld drops unnecessary services and ports"
RLCH_MODULE_DESCRIPTION="Inventories the services and ports exposed by firewalld for comparison with the server role's approved firewall baseline."
RLCH_MODULE_RATIONALE="Only services and ports required and approved for the server role should be exposed through the host firewall."
RLCH_MODULE_LEVEL="1"
RLCH_MODULE_ENABLED="true"
RLCH_MODULE_REQUIRES_REBOOT="false"
RLCH_MODULE_OPENSCAP_RULE="manual"
