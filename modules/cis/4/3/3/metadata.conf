#!/usr/bin/env bash
# CIS 4.3.3 - Ensure nftables default deny firewall policy.
# SPDX-License-Identifier: MIT

# shellcheck disable=SC2034 # Metadata is consumed by lib/module_api.sh.
RLCH_MODULE_ID="4.3.3"
RLCH_MODULE_TITLE="Ensure nftables default deny firewall policy"
RLCH_MODULE_DESCRIPTION="Verifies effective firewalld zone targets reject or drop unmatched traffic while preserving the loopback-only trusted zone."
RLCH_MODULE_RATIONALE="A default-deny firewall admits explicitly allowed traffic while rejecting or dropping unsolicited traffic that matches no approved rule."
RLCH_MODULE_LEVEL="1"
RLCH_MODULE_ENABLED="true"
RLCH_MODULE_REQUIRES_REBOOT="false"
RLCH_MODULE_OPENSCAP_RULE="manual"
