#!/usr/bin/env bash
# CIS 4.1.2 - Ensure a single firewall configuration utility is in use.
# SPDX-License-Identifier: MIT

# shellcheck disable=SC2034 # Metadata is consumed by lib/module_api.sh.
RLCH_MODULE_ID="4.1.2"
RLCH_MODULE_TITLE="Ensure a single firewall configuration utility is in use"
RLCH_MODULE_DESCRIPTION="Installs and enables firewalld while disabling the standalone nftables service so firewalld remains the sole firewall configuration utility."
RLCH_MODULE_RATIONALE="Using firewalld and the standalone nftables service concurrently can create conflicting firewall configurations."
RLCH_MODULE_LEVEL="1"
RLCH_MODULE_ENABLED="true"
RLCH_MODULE_REQUIRES_REBOOT="false"
RLCH_MODULE_OPENSCAP_RULE="manual"
